import '../game/characters/character.dart';

class CharacterAudioManager {
  void playCharacterSound(Character character) {
    // Audio implementation can be added later
  }
  
  void dispose() {
    // Cleanup if needed
  }
}

