import 'character.dart';

class ItalianBrainrotCharacters {
  static Character? getCharacterForEvent({
    required CharacterType type,
    required int score,
    required int level,
    required int linesCleared,
  }) {
    // Return null for now - can be implemented later
    return null;
  }
}

